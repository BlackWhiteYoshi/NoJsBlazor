@namespace UnitTest
@inherits BunitContext

@code {
    private class TestModel {
        public string? Text { get; set; }
    }


    private (IRenderedComponent<ContainerFragment>, EditFormInput) RenderTitle(string title) {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text" Title="@title"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput);
    }

    private (IRenderedComponent<ContainerFragment>, EditFormInput) RenderLabel(string label) {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text" Label="@label"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput);
    }

    private (IRenderedComponent<ContainerFragment>, EditFormInput) RenderType(string type) {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text" Type="@type"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput);
    }

    private (IRenderedComponent<ContainerFragment>, EditFormInput) RenderId(string id) {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text" Id="@id"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput);
    }

    private (IRenderedComponent<ContainerFragment>, EditFormInput) RenderName(string name) {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text" Name="@name"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput);
    }

    private (IRenderedComponent<ContainerFragment>, EditFormInput) RenderAutocomplete(bool enabled) {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text" Autocomplete="enabled"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput);
    }


    private (IRenderedComponent<ContainerFragment>, EditFormInput, TestModel) RenderStandard() {
        TestModel testModel = new TestModel();
        EditFormInput editFormInput = null!;

        IRenderedComponent<ContainerFragment> fragment = Render(
            @<EditForm Model="testModel">
                <EditFormInput @ref="editFormInput" @bind-Value="testModel.Text"></EditFormInput>
            </EditForm>
            );

        return (fragment, editFormInput, testModel);
    }
}
